class Solution {
public:
    int sumOfGoodIntegers(int n, int k) {
        int x=n+k;
        int sum=0;
        for(int i=x; i>=1; --i){
            if((n&i)==0 && abs(n-i)<=k){
                sum+=i;
            }
        }
        return sum;
        
    }
};
